# split() 与 strip()
    split() 用于分割指定的字符串
        >>> s = 'hello world hello' 
        >>> s.split()
        ['hello', 'world', 'hello']
    strip()  # 用于出去指定的字符串
        >>> s = 'hello world hello' 
        >>> s.split()
        ['hello', 'world', 'hello']